from company import dlg
from sclcore import do_debug
from config import run_config



def main():
    do_debug()
    file_path = 
    #get NestGetSheets()
    nest_get_sheets = 
    if nest_get_sheets:
        with open(file_path, "r") as f:
            sheets =
            for sheet in sheets:
                area = _NSheetArea
                matherial = 
                width
                height
                pieces =
                curr1 =
                curr2 =
                total_area += area
                total_left += (area * curr1) / 100

                
